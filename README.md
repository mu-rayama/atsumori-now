# 配置手順

1. `generate-data.html` をSafariまたはChromeで開く。
2. 「生成して保存」を押して `data.json` を保存する。
3. GitHubリポジトリ直下へ `index.html` と `data.json` をアップロードする。
4. `generate-data.html` はアップロード不要。

`index.html` は外部APIを参照せず、同じ階層の `data.json` だけを読み込みます。
